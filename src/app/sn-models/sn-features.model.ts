export class Feature {
    public title: string;
    public desc: string;
    public url: string;

    constructor(title: string, desc: string, url: string) {
        this.title = title;
        this.desc = desc;
        this.url = url;
    }
}
